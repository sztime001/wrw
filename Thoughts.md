Aim: a minimum viable playable game

simple controller-like game, no websocket required (initial stage).

user can send request via their phone, server only listen the request, and change state base on the request from certain people

can be a terminal to display the information(Game start ...)

required module:

- a api to handle request (flask)
- some sort of time counter



